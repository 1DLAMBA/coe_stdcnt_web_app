# Create React App loads this file when you run `react-scripts start` directly (some IDEs).
# `npm start` uses env-cmd with environments/.env.development instead; keep both in sync.
REACT_APP_BACKUP_API_BASE_URL=http://127.0.0.1:8001/api
