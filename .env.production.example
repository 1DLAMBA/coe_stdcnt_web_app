# Production environment variables for Vercel (and other hosts).
# Do NOT commit real keys. Set these in Vercel: Project → Settings → Environment Variables.
# Use "Production" environment when adding them.

# Backend API base URL (your deployed Laravel API, no trailing slash)
REACT_APP_API_BASE_URL=https://your-api-domain.com/api

# Paystack public key – use LIVE key in production
# Get from: https://dashboard.paystack.com/#/settings/developers
REACT_APP_PAYSTACK_PUBLIC_KEY=pk_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- Backend only (Laravel .env). Never add PAYSTACK_SECRET_KEY to frontend. ---
# PAYSTACK_SECRET_KEY=sk_live_...
# Paystack verification is done via your backend /api/verify-paystack/{reference}
