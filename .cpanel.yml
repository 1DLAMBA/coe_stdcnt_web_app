---
deployment:
  tasks:
    - export DEPLOYPATH=/home/coestudy/coe_web_app 
    - /bin/cp -R ./* $DEPLOYPATH
    - cd $DEPLOYPATH
    - npm install
    - npm run build
    - /bin/cp -R build/* $DEPLOYPATH
    - /bin/rm -rf node_modules